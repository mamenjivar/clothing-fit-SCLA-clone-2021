import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fit-SCLA-clone-2021',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
